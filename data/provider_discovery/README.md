# Provider Discovery & Coverage Audit Reports

Generated artifacts from `catalyst_data/coverage_audit.py` — read-only audit of
the dev DB state. Each run produces a timestamped JSON report.

These files are gitignored. Re-run `run_coverage_audit()` to regenerate.
