# 第五章（结论与展望）重写设计稿（结果优先）

日期：2026-05-21  
范围：本科论文第5章（结论与展望）  
目标文件：`docs/thesis/01_final_for_paste/50_ch5_conclusion_and_outlook_draft_v1.md`

## 1. 写作目标

将第5章从“方案介绍式总结”重写为“结果锚定式总结”：在不要求读者回翻第4章的前提下，直接给出本次实验最硬的结论数字，并用同证据对照与消融结果解释增益来源与边界，最后给出可执行的后续改进方向。

约束与偏好：
- 全中文为主，减少工程文件名/路径/指标键名（如 `.json/.csv`、`refusal_precision`）在正文出现。
- 章节命名统一使用 **EventUS**；首次可括注“工程实现为 Catalyst”，其余不反复使用 Catalyst。
- 不在第5章重复第3章“架构堆节点名”的描述；若需提及流程，使用功能描述（证据挖掘/充分性判定/状态路由/生成/后验校验/状态收敛）。
- 第5章引用支撑统一指向第4章表图：`表4-2/表4-3、图4-1~图4-4`。

## 2. 可直接写入第5章的“结果锚点”（来自第4章定稿）

主结论采用公共样本口径：共同样本 `n=99`，可归因样本 `N_eligible=93`（见第4章口径说明）。

核心对比（见表4-2、图4-1）：
- tier1：归因正确率 `0.0753 (7/93)`；有效归因率 `0.1010 (10/99)`；状态判定正确率 `0.1818`
- tier2：归因正确率 `0.3656 (34/93)`；有效归因率 `0.4747 (47/99)`；状态判定正确率 `0.8081`
- EventUS：归因正确率 `0.5484 (51/93)`；有效归因率 `0.8788 (87/99)`；状态判定正确率 `0.8384`

需在结论中明确的“增益解释口径”：
- tier1→tier2：主要体现“证据供给”带来的跃迁（同任务下，注入更强证据显著提升）。
- tier2→EventUS（同证据对照）：状态准确率仅边际提升（`+3.0pp`），但人工归因主指标为实质提升：
  - 归因正确率：`+18.3pp`（`0.5484 - 0.3656`）
  - 有效归因率：`+40.4pp`（`0.8788 - 0.4747`）
结论要点：EventUS 的主要收益是“输出可用性/可复核性”，而非仅“状态标签更准一点”。

错误类型结构性差异（见表4-3，可用于“边界”段落）：
- tier1 主要失败为过度拒答（`67/99`）。
- tier2 主要失败为表述空泛（`41/99`）与主因错配（`12/99`）。
- EventUS 过度拒答显著下降（`3/99`），但主因错配仍为主要残留问题（`33/99`）。

拒答能力补充对比（见图4-4，用于“风险控制能力”一句话总结）：
- EventUS vs tier2：拒答准确率 `0.667 vs 0.353`；应拒答命中率 `0.970 vs 0.889`；拒答召回率均为 `1.0`。

Critic 启用/停用收益-代价（见图4-3，用于“权衡与后续优化方向”）：
- 状态判定正确率：启用 Critic 相比停用下降 `11.0pp`（`0.75` vs `0.86`）。
- 代价：令牌数 `+44.2%`、时延 `+10.6%`、成本 `+60.7%`（相对变化）。
写法建议：不把 Critic 结论写成“无用/失败”，而写成“有方法学价值但当前触发策略与阈值仍需优化，且存在显著代价”。

工程代价（直接问答基线）结论（见图4-2）：
- tier0/tier1/tier2 平均令牌数：`349.6 / 749.0 / 2377.6`
- 平均时延（ms）：`3254.1 / 5780.1 / 6326.8`
在第5章中只需一句话：直接问答基线的证据增强带来明显代价上升；EventUS 本批次缺少按样本粒度代价记录，后续需补齐采集。

## 3. 第5章结构设计（结果优先）

### 5.1 结论（3段，强数字开头）

段落1（主结果一句话定性 + 一组数字）：
- 任务一句话：将股价异动解释从“能写”提升到“可审计、可核验、可复盘”。
- 直接写入 `n=99/eligible=93` 口径 + 三方法主结果数字。
- 明确：相对 tier2 的 `+18.3pp/+40.4pp` 为本文最核心贡献结论。
- 末尾加引用：见表4-2、图4-1。

段落2（增益来源：证据供给 vs 流程治理）：
- 用 tier1→tier2 说明“证据供给决定能否起步”。
- 用 tier2→EventUS（同证据）说明“流程治理是质量放大器”：把可答性判断前置、状态路由显式化、后验校验与资产沉淀带来更高“可用解释输出比例”。
- 引用：表4-2、表4-3。

段落3（边界 + 权衡 + 稳健性）：
- 边界：EventUS 仍存在主因错配（表4-3 的结构性残留）。
- 稳健性：拒答能力在同证据条件下优于 tier2（图4-4）。
- 权衡：Critic 的启用/停用结果表明当前策略仍需优化且有明显代价（图4-3），并强调后续要做“成本-收益”完整评估（补齐 EventUS 侧代价采集）。

### 5.2 展望（3条，合并与落地）

展望1（合并主因对齐 + 证据覆盖，避免两条重复）：
- 目标：减少“可解释但主因错配”，提高多事件并发/语义相近场景的主因对齐稳定性；同时提升证据覆盖与时间对齐能力。
- 可执行方向：事件级重排、跨证据一致性约束、更细粒度时间窗与跨源对齐规则、扩充数据源白名单。

展望2（工程代价与稳定性）：
- 补齐按样本粒度的令牌/时延/成本采集（EventUS 主链路多次调用，需量化）。
- 优化方向：缓存、并行调度、触发策略收敛、异常恢复与格式稳定性。

展望3（长期评测与回归机制）：
- 从一次性离线批评测扩展为周期性回归：漂移检测、报警阈值、固定样本集回归与新增样本增量评测。

## 4. 改写清单（最少改动达成目标）

- 5.1 第一段：新增“结果锚点段”（含 `n=99/eligible=93` 与表4-2主数字）。
- 5.1 中部：将“Miner/Critic/Judge/Validator/Finalizer”组件列举改为功能描述（证据挖掘/充分性判定/结论生成/后验校验/状态收敛）。
- 5.1 末段：新增“边界与权衡”句群，并引用图4-3、图4-4。
- 5.2：将原展望第1、第2条合并为一条（主因对齐 + 证据覆盖），其余展望精简为三条。

## 5. 成功判据（写完后自检）

- 读者只读第5章即可复述三条硬结论：提升幅度（+18.3pp/+40.4pp）、增益来源（同证据对照说明流程治理）、主要边界（主因错配仍高）。
- 第5章不出现任何文件名/路径/指标键名（如 `.json/.csv`、`refusal_precision`、`attribution_f1`）。
- 第5章每段都有明确的第4章表图引用承接：表4-2/表4-3/图4-1~图4-4。

