# M4 — Foundation Path Authoritative Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make Observation/MoveProfile, bounded research, EvidenceState, CoverageSummary, and persisted ContextPack the only Analyst inputs, with production identity/hash/replay gates — replacing the legacy context/miner mutable state as the authoritative foundation.

**Architecture:** Evolve `ContextBuilder` formulas into agents-owned `ObservationBuilder`; evolve the Miner retrieval seam into `ResearchExecutor` over typed `ResearchTask`s; replace mutable list state with immutable `EvidenceState` + deterministic `CoverageSummary` + `ContextPackBuilder`; persist the exact pack and rendered messages as immutable artifacts (Final TSD §7).

**Tech Stack:** Python 3.11+, Pydantic v2, SQLite (read-only factories for data-core; connection-per-operation), existing `ContextBuilder`/`SQLiteContextProvider`/`AgentRetrieverAdapter`, pytest.

**Normative authority:** Frozen V1.1 §6.1–6.3. **Implementation authority:** Final Migration TSD §6, §7, §27 M4; Phase 2/3 TSD §4–21, §23–24.

---

## Milestone contract (Final TSD §27 M4)

| Field | Contract |
|---|---|
| WHAT BECOMES TRUE | Observation/MoveProfile, bounded research, EvidenceState, CoverageSummary, and persisted ContextPack are the only Analyst inputs |
| DEPENDENCIES | M2; M3 for production binding |
| MAJOR WORK | Wire repaired retrieval/structured data, deterministic merge/packing, exact persistence/replay |
| EXIT GATE | Production identity, state union, pack/render hash, budget, and provenance tests |
| LEGACY REMOVED | Context/miner mutable state becomes baseline-only |
| ROLLBACK | Select sealed baseline revision/config, never mixed state |
| PARALLEL WORK | Analyst/claim logic; app event/SSE against fake producer |

## Repo-reality revalidation

- Confirm M3 tag (`v1.1-m3-data`) exists for production binding; fixture mode is available before M3 if needed.
- Confirm current symbols: `ContextBuilder.build` in `packages/agents/catalyst_agents/attribution/context_builder.py`, `SQLiteContextProvider` in `runtime/context.py`, `AgentRetrieverAdapter` in `runtime/retrieval_adapter.py`, `ProductionHybridRetriever` in data-core `retrieval/hybrid.py`, `AttributionState` in `state.py`.
- Confirm `SQLiteContextProvider` currently supplies OHLCV-only inputs (benchmark/sector/peer absent) — M4 wires whatever structured context M3 certified; missing proxies stay explicit unknowns (Q-008).
- If retrieval/metadata contract from M3 is not yet bound to the runtime, fixture mode must be used and production binding deferred to a documented partial gate.

## Starting-state checks

```bash
cd /Users/yiannischen/Projects/Catalyst-v1.1-m4
git branch --show-current                                 # v1.1/m4-foundation-path
git status --porcelain                                    # clean
git rev-parse v1.1-m3-data
test "$(git rev-parse HEAD)" = "$(git rev-parse v1.1/integration)"
git merge-base --is-ancestor v1.1-m3-data HEAD
git diff --stat v1.1-m3-data -- packages apps configs     # empty at M4 start
git log --oneline -3                                      # M3 merge/tag is the base
git worktree list
.venv/bin/python -m pytest packages/agents -q -m "not integration"
```
Create the worktree from `v1.1/integration` before these checks: `git worktree add ../Catalyst-v1.1-m4 v1.1/m4-foundation-path`.

---

## Task M4-1: ObservationBuilder + MoveProfile implementation

**Files:**
- Modify: `packages/agents/catalyst_agents/attribution/context_builder.py` (add `ObservationBuilder`, `build_move_profile`)
- Modify: `packages/agents/catalyst_agents/attribution/provider.py` (structured input protocol: PIT facts only)
- Modify: `packages/agents/catalyst_agents/runtime/context.py` (SQLite structured reader; benchmark/sector/peer/macro/fundamental tables when present)
- Create: `packages/agents/tests/test_observation_builder.py`

**Step 1: Write the failing test** (FAST, deterministic, no LLM)
- `ObservationBuilder(provider).build(ticker, session_date, cutoff, runtime_identity) -> MoveProfile` computes: target_return, prior_session_return, gap_return (session open − prior close), market_return, sector_return, peer_summary (stable median over present peers; `PARTIAL` coverage when some peers missing), market/sector/peer_adjusted_return, volume_abnormality (band NORMAL|ELEVATED|EXTREME|UNKNOWN; never NORMAL when volume data missing), scheduled_macro_flags, typed `SessionAlignment` for market/sector/peer co-move, coverage_flags[], degraded_fields[].
- Missing benchmark → null market fields + UNKNOWN alignment; missing sector does not erase market residual; after-close TemporalIdentity uses correct session boundary; session validity distinct from target-return availability.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** by reusing existing ContextBuilder formulas (returns/volume/decomposition) and adding typed MoveProfile assembly; keep `ContextBuilderArtifact` for baseline reads only (legacy node/name removed at M5).

**Step 4: Run tests** → PASS (new + existing `test_context_builder.py`).

**Step 5: Commit**
```bash
git add packages/agents/catalyst_agents/attribution/context_builder.py packages/agents/catalyst_agents/attribution/provider.py packages/agents/catalyst_agents/runtime/context.py packages/agents/tests/test_observation_builder.py
git commit -m "feat(agents): ObservationBuilder and MoveProfile production formulas"
```
**Rollback:** `git revert`; baseline ContextBuilder remains readable.

## Task M4-2: Scenario classification + InitialResearchPolicy

**Files:**
- Modify: `packages/agents/catalyst_agents/retrieval/policy.py` (replace `Layer` semantics with typed policy)
- Create: `packages/agents/tests/test_initial_research_policy.py`

**Step 1: Write the failing test**
- `InitialResearchPolicy(config).classify(move_profile) -> list[ResearchTask]` implements Frozen §6.2 precedence exactly: SCHEDULED_MACRO → MACRO_EVENT×SESSION + MACRO_SERIES×SESSION + COMPANY_NEWS×SESSION sanity; CONTINUATION → COMPANY_PRIMARY×PRIOR + COMPANY_NEWS×PRIOR + COMPANY_NEWS×SESSION sanity; BROAD_SECTOR → SECTOR_NEWS×SESSION + MACRO_EVENT×SESSION + COMPANY_NEWS×SESSION sanity; COMPANY_SPECIFIC → COMPANY_PRIMARY×SESSION + COMPANY_NEWS×SESSION; QUIET_OR_UNCLASSIFIED → at most COMPANY_NEWS×SESSION + COMPANY_PRIMARY×SESSION.
- Max 3 tasks; dedup identical tasks; thresholds are config (pinned in RunManifest), not constants; CONTINUATION must not depend on news/evidence; MARKET_STRUCTURE produces a capability gap, not a retrieval task; missing structured data falls to quiet/unclassified.
- Test exact task set/order for every scenario, including overlapping predicates (precedence wins).

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** the policy + config schema (`packages/agents/catalyst_agents/runtime/config.py` or inline dataclass); keep `retrieval/policy.py`'s retriever seam intact.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/retrieval/policy.py packages/agents/tests/test_initial_research_policy.py`
`git commit -m "feat(agents): deterministic scenario classification and initial research policy"`

## Task M4-3: ResearchExecutor (bounded parallel tasks + deterministic merge)

**Files:**
- Create: `packages/agents/catalyst_agents/retrieval/execution.py`
- Create: `packages/agents/tests/test_research_execution.py`

**Step 1: Write the failing test**
- `ResearchExecutor(retriever, deadline, limiter).execute(tasks, ...) -> EvidenceState` runs independent tasks with bounded parallelism under one shared stage deadline; per-task degradation recorded; identity/integrity failure fails the run (not hidden by sibling success).
- Deterministic merge order: task priority, task ID, within-task rank, then evidence ID — completion timing never determines order. Test with out-of-order completion (fixture retriever with controllable delays).
- Zero hits is a valid empty task result; optional unsupported need = capability gap; `MARKET_STRUCTURE` never creates a retrieval action.
- Deadline exceeded → `RESEARCH_DEADLINE` technical failure, not ABSTAIN.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** a thin executor over `AgentRetrieverAdapter`/`ProductionHybridRetriever` with injected resource limits; no run admission/lifecycle ownership (Final TSD §15).

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/retrieval/execution.py packages/agents/tests/test_research_execution.py`
`git commit -m "feat(agents): bounded research task executor with deterministic merge"`

## Task M4-4: EvidenceState builder (cumulative union)

**Files:**
- Create: `packages/agents/catalyst_agents/attribution/evidence_state_builder.py`
- Create: `packages/agents/tests/test_evidence_state_builder.py`

**Step 1: Write the failing test**
- `build_evidence_state(results_by_task, prior: EvidenceState | None) -> EvidenceState` performs immutable cumulative union by evidence/fact ID; same chunk merges contributions and keeps all task IDs; same chunk across rounds keeps first-seen round; multiple chunks from one article remain multiple items; syndicated assets remain separate assets in one group; conflicting immutable metadata (e.g., two provider rows with different `eligible_at` for the same chunk) fails integrity validation.
- Round 2 = dedup(round1 ∪ new); round-two MUST NOT discard round-one evidence.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** pure builder over M2 `evidence_state.py` schemas.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/attribution/evidence_state_builder.py packages/agents/tests/test_evidence_state_builder.py`
`git commit -m "feat(agents): cumulative evidence state builder"`

## Task M4-5: CoverageSummary builder

**Files:**
- Create: `packages/agents/catalyst_agents/attribution/coverage_builder.py`
- Create: `packages/agents/tests/test_coverage_builder.py`

**Step 1: Write the failing test**
- `build_coverage_summary(evidence_state, move_profile, retrieval_degradations, capability_registry) -> CoverageSummary` computes every observable field from Final TSD §6.4 with distinct-asset semantics for role counts; known independence-group count excludes unknown lineage; content-state counts exact; parse degradation counted without changing materiality; retrieval degradation and data/capability gaps separate; no semantic support fields exist (schema-negative test re-asserted).
- `material_evidence_count` interpretation (clarification #1): pre-Analyst material-capable counts only.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** pure builder.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/attribution/coverage_builder.py packages/agents/tests/test_coverage_builder.py`
`git commit -m "feat(agents): observable coverage summary builder"`

## Task M4-6: ContextPackBuilder (deterministic packing)

**Files:**
- Create: `packages/agents/catalyst_agents/attribution/context_pack_builder.py`
- Create: `packages/agents/catalyst_agents/runtime/token_budget.py` (tokenizer registry; conservative UTF-8 byte counter fallback)
- Create: `packages/agents/tests/test_context_pack_builder.py`

**Step 1: Write the failing test**
- `ContextPackBuilder(config).build(run, temporal_identity, data_runtime_identity, evidence_state, move_profile, coverage_summary, policy_hashes, round) -> EvidenceAnalystContextPack` implements Final TSD §6.5 ordering: validate → inventory every unique item/fact with identity/safety metadata → exclude post-cutoff/EMPTY/FAILED → exclude metadata-only body (retain bounded title/lead under ceiling) → order by task priority + retrieval/rerank utility → source-role ceiling → canonical/group novelty → temporal relevance → token efficiency → allocate fixed observation/coverage/research-history/inventory/payload budgets → select exact retrieved excerpts (never corpus scan, never LLM) → compress duplicate/syndicated news to one representative body with every member in inventory → preserve distinct filing sections → truncate only via registered tokenizer offsets or declared conservative counter with `truncation_metadata` (original/included counts + offsets).
- Round-two: prioritize structurally new evidence + prior Analyst-identified counter-evidence/conflicts while retaining cumulative inventory; `prior_*` semantic fields reference the persisted prior AnalystDecision/EvidenceAssessment only.
- Tests: ordering identical across task completion orders; rendered token invariant ≤ budget; every included/excluded item inventoried; canonical chain survives; SEC section identity survives truncation; news duplicate compression deterministic; round-two delta priority; canonical serialization/hash replay byte-identical; exact counter vs proxy counter vs UTF-8 fallback labeled.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** pure builder + token budget registry; register BGE-M3/chat tokenizer adapters where available; unsupported exact-budget mode must label fail-closed proxy mode (clarification #5).

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/attribution/context_pack_builder.py packages/agents/catalyst_agents/runtime/token_budget.py packages/agents/tests/test_context_pack_builder.py`
`git commit -m "feat(agents): deterministic context pack builder with budget and replay"`

## Task M4-7: ContextPack persistence + rendered-message artifacts

**Files:**
- Create: `packages/agents/catalyst_agents/runtime/pack_persistence.py` (pack/render hash contract + persistence seam protocol)
- Create: `packages/agents/tests/test_pack_persistence.py`
- Do **NOT** add V1.1 artifact envelope tables or new-write paths to `packages/agents/catalyst_agents/trace/artifacts.py` in M4; agents defines the contract, app owns the V1.1 `run_artifacts` write envelope in M6 (no dual-write path, no dual event/artifact schema owner).

**Step 1: Write the failing test**
- `persist_pack_and_render(run_id, pack, rendered_messages, template_version) -> PersistedPackRefs` writes two immutable artifacts: exact serialized `EvidenceAnalystContextPack` (with `context_pack_sha256`) and exact normalized rendered model messages (excluding transport credentials/headers) with `prompt_template_version`, `prompt_template_sha256`, `rendered_messages_sha256`.
- **Authority test (Final TSD §7):** the pair must commit before a provider request is dispatched; a rebuild is valid only as a replay assertion reproducing both hashes; missing artifacts ⇒ run non-replayable (cannot support eval/assurance); reconstruction never becomes authority.
- Hash replay: rebuild from canonical Observation/EvidenceState reproduces both hashes byte-identically.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** the pack/render hash contract and a `PackPersistence` protocol (persist → refs; replay asserts hash equality). Production new-write artifact persistence is the M6 app-owned `run_artifacts` envelope; agents do not write V1.1 run_artifact rows in M4 — the fixture store is test-only.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/agents/catalyst_agents/trace/artifacts.py packages/agents/catalyst_agents/runtime/pack_persistence.py packages/agents/tests/test_pack_persistence.py`
`git commit -m "feat(agents): persisted context pack and rendered-message authority"`

## Task M4-8: Foundation graph adapter (fixture mode) + legacy state baseline-only

**Files:**
- Modify: `packages/agents/catalyst_agents/graph.py` (add `build_foundation_graph` or a `foundation` config seam; do not delete MCJ yet)
- Modify: `packages/agents/catalyst_agents/state.py` (add V1.1 reference fields; keep legacy fields readable)
- Create: `packages/agents/tests/test_foundation_graph_fixture.py`

**Step 1: Write the failing test**
- `build_foundation_graph(...)` wires: `observation_build → research_policy → research_execution → evidence_state → coverage_summary → context_pack_build` and terminates at a fixture stub for the Analyst boundary (M5 replaces the stub). It does not run Miner/Critic/Judge.
- State carries artifact references/hashes (run/stage, immutable artifact refs, round/attempt counters, deadline/cancel, terminal error) — no copied evidence/causes/prose/reasoning in `AttributionState` V1.1 fields (Final TSD §3 "Graph state").
- Offline fixture run: fake retriever + fake structured provider; assertions on EvidenceState union, CoverageSummary counts, pack hash replay.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** a foundation graph in `graph.py` beside the MCJ graph; mark MCJ production imports `BASELINE_ONLY` in comments (no deletion until M5 comparison gate).

**Step 4: Run tests** → PASS (new + existing `test_graph.py`).

**Step 5: Commit**
`git add packages/agents/catalyst_agents/graph.py packages/agents/catalyst_agents/state.py packages/agents/tests/test_foundation_graph_fixture.py`
`git commit -m "feat(agents): foundation graph adapter; legacy context/miner state baseline-only"`

## Task M4-9: Exit gate + integration

**Step 1: Run exit-gate verification**
```bash
cd /Users/yiannischen/Projects/Catalyst
.venv/bin/python -m pytest packages/agents -q -m "not integration"
.venv/bin/python -m pytest packages/agents/tests/test_observation_builder.py packages/agents/tests/test_context_pack_builder.py packages/agents/tests/test_pack_persistence.py packages/agents/tests/test_foundation_graph_fixture.py -v
git diff --check
```
Expected: production identity, state-union, pack/render hash, budget, and provenance tests green; legacy mutable state fields are baseline-only (no new production writes); MCJ graph still compiles for the M1 baseline instrument.

**Step 2: Integrate**
```bash
git checkout v1.1/integration && git merge --no-ff v1.1/m4-foundation-path -m "merge(v1.1): M4 foundation path authoritative"
git tag -a v1.1-m4-foundation -m "V1.1 M4 foundation path"
```

**Step 3: Cleanup**
```bash
git worktree remove ../Catalyst-v1.1-m4
git worktree prune
git branch -d v1.1/m4-foundation-path
```

**Rollback:** select sealed baseline revision/config (M1 tag), never mixed state; `git revert` focused commits if needed.

## Exit gate summary (M4)

- [ ] MoveProfile/ObservationBuilder deterministic tests green (no LLM)
- [ ] InitialResearchPolicy ≤3 tasks per Frozen precedence
- [ ] EvidenceState cumulative union + integrity conflict tests green
- [ ] CoverageSummary observable fields; schema-negative test proves no support/causality fields
- [ ] ContextPack byte-identical hash replay; budget invariant; round-2 delta semantics
- [ ] Persisted pack + rendered messages authoritative; rebuild is replay-only
- [ ] Legacy context/miner mutable state baseline-only; MCJ still compiles
- [ ] Tag `v1.1-m4-foundation` present
