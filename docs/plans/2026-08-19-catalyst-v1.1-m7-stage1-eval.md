# M7 — Stage-1 Operational Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Run twelve hidden, human-reviewed cases through one manifest-bound V1.1 path with interpretable metrics (retrieval, attribution, trajectory), A1/A2/A3/A5 development signals, A4 readiness only, T4/user-smoke regression, and a reproducible Stage-1 report with no gold leakage.

**Architecture:** GoldenCase + EvalManifest are eval-owned and immutable; human labels are primary truth; LLM judges are diagnostics only; every metric publishes numerator/denominator/eligible/excluded counts. The M6 app/AdmissionController and the single `run_v1_graph` implementation remain authoritative. Eval-only policy overrides use explicit seams on that runner and are rejected by production composition. Stage-1 is a development probe, never an efficacy claim; A4 is schema/eligibility readiness only and A3/A4 architecture conclusions require Stage-2-or-later denominators.

**Tech Stack:** Python 3.11+, Pydantic v2, JSONL datasets, pytest, existing `packages/eval` harness/metrics, four-arm retrieval runner.

**Normative authority:** Frozen V1.1 §7, §10. **Implementation authority:** Final Migration TSD §22–23, §27 M7; Phase 6 TSD §6–19, §23.

---

## Milestone contract (Final TSD §27 M7)

| Field | Contract |
|---|---|
| WHAT BECOMES TRUE | Twelve hidden human-reviewed cases run through one manifest-bound V1.1 path with interpretable metrics |
| DEPENDENCIES | M3–M6 plus GoldenCase/EvalManifest |
| MAJOR WORK | Stage-1 A1/A2/A3/A5 development signals, A4 schema and eligibility-manifest readiness only, T4/user-smoke, and migration regression |
| EXIT GATE | Hard gates, all denominators/eligible counts, no leakage, reproducible reports/artifacts |
| LEGACY REMOVED | Legacy eval becomes sealed baseline reader only |
| ROLLBACK | Re-run sealed prior generation/path by manifests; label non-comparable identities |
| PARALLEL WORK | Stage-2 annotation and Stage-3 holdout preparation |

## Repo-reality revalidation

- Confirm M6 tag (`v1.1-m6-runtime`); app/SSE/executor path live; M3 repaired generation serving.
- Confirm eval package still has `benchmark/case.py`, `benchmark/judgment.py`, `benchmark/lineage.py`, `benchmark/pool_manifest.py`, `post_import/{case_pack,t4_evidence,four_arm,user_smoke}.py`, `golden_set/pre_b6_attribution_cases_v1.jsonl` (12 rows, lacks GoldenCase fields), `v1_2_p0_set.jsonl`, `h_refusal_cases.validated.json`.
- Confirm `run_post_import_user_smoke.py` still requires the 10-case Wave-2 evidence package and selects g006/g007/h004.
- The integrated M6 baseline currently has 14 `packages/eval/tests/test_post_import_user_smoke.py` failures: the fixture injects legacy `LiveRunService`, while `POST /api/live-runs` now uses M6 `AdmissionController` composition. This is M7-0 and must be fixed without restoring the legacy route.
- If the Stage-1 12-case list is not yet human-reviewed (Q-011), implementation may proceed through fixture-backed contracts and metrics, but operator execution stops at the M7-2 seal gate. The executor must not invent human approval.

## M7 execution lock

### Canonical serialization and identities

All new M7 hashes use exactly:

```python
def canonical_bytes(value: object) -> bytes:
    return json.dumps(
        value,
        sort_keys=True,
        separators=(",", ":"),
        ensure_ascii=False,
        allow_nan=False,
    ).encode("utf-8")
```

The hash is lowercase `sha256(canonical_bytes(payload)).hexdigest()`.

- `case_list_sha256`: payload is the ordered JSON array of `case_id` strings. Never sort it.
- `dataset_content_sha256`: payload is the ordered array of `GoldenCase.model_dump(mode="json")` rows. README, filesystem path, whitespace, and the dataset manifest itself are excluded.
- `eligibility_hash`: payload is `{"experiment_id": experiment_id, "eligibility_predicate_version": eligibility_predicate_version, "ordered_eligible_ids": list(ordered_eligible_ids), "minimum_eligible_denominator": minimum_eligible_denominator}`. Never derive eligibility after outcomes exist.
- `eval_id`: hash the complete pre-execution identity payload: evaluation identity excluding `eval_id`, code/provider identity, data identity reference, retrieval policy, agent policy, metric contract, eligible experiments, and the empty planned run/artifact identity. Outcomes, timestamps, local paths, credentials, and observed run bindings are excluded.
- `run_manifest_hash` and artifact hashes are hashes of their existing authoritative canonical serialized bytes; M7 references them and never redefines their identities.

### EvalManifest lifecycle

The existing M2 `EvalManifest` remains the pre-execution immutable plan. For live runs its top-level `run_artifact_identity` is empty. M7 evolves `EvalOutcome` with `observed_run_artifact_identity: RunArtifactIdentity`, and evolves `CaseResultRef` with `run_manifest_hash`. The M6 pre-submit hook captures each authoritative RunManifest ID/hash before provider dispatch; terminal execution contributes result/ContextPack/claim/assurance refs. After every expected case reaches a terminal state, `append_outcome` is called exactly once.

The ordered observed bindings must match ordered `case_ids` one-to-one. Missing, duplicate, reordered, identity-mismatched, or extra bindings fail closed. No identity field is mutated after execution begins. Checkpoints are separate untracked execution-ledger artifacts; they are not rewritten as EvalManifest identity.

### Human and operator gates

| Gate | Blocks | Does not block |
|---|---|---|
| M7-0 Eval FAST green | all M7 implementation after M7-0 | plan amendment |
| Q-011 approved 12-case manifest | authoritative M7-2 seal and all live Stage-1 execution | fixture-backed loader/manifest/metric/CLI TDD |
| Q-001 exact operational derivative identity | live execution and reproducibility claims | local deterministic tests |
| Q-002 unrecovered | comparable claim | explicitly NON-COMPARABLE execution/report |
| DATA-02/Q-004 incomplete news bodies | news-attribution denominator | coverage-limited reporting and non-news cases |
| Human output audit | attribution hard-gate seal | live run capture and automatic system metrics |

### Metric formula lock

- Aggregation is macro over eligible cases unless the metric explicitly names returned units/claims as its denominator; every aggregate carries integer numerator, denominator, eligible, excluded, and non-scorable counts plus case IDs.
- Recall@8 is `sum(relevant_unique_units_hit / relevant_unique_units) / eligible_cases`. MRR uses the first relevant unique unit rank. nDCG@8 uses gains `primary_support=3`, `secondary_support=2`, `contradiction=1`, all other roles `0`, binary temporal eligibility, and `log2(rank + 1)` discount.
- Primary-source hit is eligible cases containing at least one expected primary unit at rank <= 8 divided by cases with expected primary evidence.
- Duplicate-adjusted Precision@8 is acceptable unique human independence groups returned at rank <= 8 divided by all unique returned groups at rank <= 8. An ungrouped unit is its own group. Empty denominators are non-scorable, never zero or pass.
- Citation correctness, causal support, and unsupported-material decisions come only from the sealed human output-audit artifact. LLM judges remain diagnostics and cannot satisfy a hard gate.
- Percent thresholds are evaluated by integer cross multiplication; no rounded display value decides a gate.

### Experiment boundary lock

- A1/A2/A3/A5 use the same `run_v1_graph` and one eval-owned adapter. Add only the minimum named configuration seams needed by the experiment; default values must reproduce M6 policy identity and behavior.
- Production `RunManifest` invariants remain `max_corrective_rounds=1` and `max_actions_per_batch=1`. Eval-only overrides are recorded in EvalManifest, never represented as a production RunManifest, and `packages/app` production composition must reject them.
- A4 in M7 creates and validates only its schema, eligibility predicate, ordered eligible IDs, minimum denominator, and refusal reasons. It does not execute `max_actions_per_batch=2`, dispatch two actions, or support a promotion conclusion.
- No second graph, second corpus builder, case-ID behavior switch, or gold-aware runtime adapter is permitted.

### Execution phases and stop rules

1. M7-0: repair the M6-to-Eval fixture boundary and make Eval FAST green.
2. M7-1 through M7-9: TDD implementation using synthetic hidden-gold fixtures. M7-2 may prepare candidate files, but cannot seal human approval without Q-011.
3. Operator prepare: verify Q-011, data/runtime identity, provider configuration, case hashes, budgets, and resumable ledger; no provider calls.
4. Operator execute: run live cases once, resume only from identity-valid terminal checkpoints, and write automatic artifacts.
5. Human audit: review generated claims/citations without modifying run artifacts, then seal the audit file.
6. Report: join only sealed run artifacts and sealed human audit, evaluate gates, scan leakage/secrets, and publish write-once report.
7. Stop for one supervisor review. Executor does not merge, tag, push, delete the branch, or remove the worktree.

Hard stops: repo reality contradicts this lock; import resolves outside the worktree; M7-0 remains red; Q-011 is absent at live execution; identity/hash mismatch; gold reaches runtime; credential appears in an artifact; cost ceiling would be exceeded; a required human decision is absent; any hard gate command exits non-zero. Do not guess or weaken a gate.

## Starting-state checks and worktree environment

Run first from main. Tracked changes are a hard stop; `scratch/` and `scripts/arclaude` may remain untracked but must never be staged:

```bash
cd /Users/yiannischen/Projects/Catalyst
test "$(git branch --show-current)" = "v1.1/integration"
git merge-base --is-ancestor v1.1-m6-runtime HEAD
test -z "$(git diff --name-only v1.1-m6-runtime HEAD -- packages apps configs)"
test -z "$(git status --porcelain --untracked-files=no)"
git worktree add -b v1.1/m7-stage1-eval \
  ../Catalyst-v1.1-m7 v1.1/integration
cd /Users/yiannischen/Projects/Catalyst-v1.1-m7
python3 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install -e ./packages/data-core
.venv/bin/python -m pip install -e ./packages/agents
.venv/bin/python -m pip install -e ./packages/app
.venv/bin/python -m pip install -e ./packages/eval
```

Run this complete block once immediately after setup. Before every later batch and the exit gate, rerun the import assertion, `git status --porcelain`, and the M6 ancestor check; the initial zero-code-drift assertion is not expected to remain empty after intentional M7 code commits:

```bash
cd /Users/yiannischen/Projects/Catalyst-v1.1-m7
.venv/bin/python - <<'PY_ASSERT'
import pathlib
import catalyst_data, catalyst_agents, catalyst_app, catalyst_eval
root = pathlib.Path.cwd().resolve()
for module in (catalyst_data, catalyst_agents, catalyst_app, catalyst_eval):
    path = pathlib.Path(module.__file__).resolve()
    assert root in path.parents, f"{module.__name__} resolved outside M7: {path}"
print("M7 worktree imports OK")
PY_ASSERT
git branch --show-current                                 # v1.1/m7-stage1-eval
git status --porcelain                                    # clean
git rev-parse v1.1-m6-runtime
git merge-base --is-ancestor v1.1-m6-runtime HEAD
git diff --stat v1.1-m6-runtime -- packages apps configs  # empty at M7 start
git worktree list
.venv/bin/python -m pytest packages/eval -q -m "not integration"
```

The last command is expected to be RED only at the initial M7-0 reproduction. It must be green before M7-1. Use one M7 worktree; do not create parallel branches for this milestone.

---

## Task M7-0: Reconcile Eval user-smoke with the M6 runtime

**Files:**
- Modify: `packages/eval/tests/test_post_import_user_smoke.py`
- Modify only if the fixture needs a reusable public seam: `packages/app/tests/v1_helpers.py`
- Modify only for a genuine production contradiction: `packages/app/catalyst_app/main.py`, `packages/app/catalyst_app/runtime/composition.py`

**Step 1: Preserve the RED evidence**

Run `.venv/bin/python -m pytest packages/eval/tests/test_post_import_user_smoke.py -q`. Expected before fix: 14 HTTP 503 failures on `/api/live-runs`.

**Step 2: Implement the minimum fixture reconciliation**

Build the smoke app with the M6 V1 `AdmissionController`/runtime dependencies and fake graph/provider seams. Keep `/api/live-runs` on the M6 production route. Do not restore legacy submission routing, bypass credential admission, perform network calls, or weaken failure-path assertions.

**Step 3: Verify and commit**

```bash
.venv/bin/python -m pytest packages/eval/tests/test_post_import_user_smoke.py -q
.venv/bin/python -m pytest packages/eval -q -m "not integration"
git add packages/eval/tests/test_post_import_user_smoke.py
# Add an app helper only if it was actually required.
git commit -m "test(eval): bind user smoke to M6 runtime composition"
```

Expected: both test commands exit 0. Stop if the fix requires weakening production admission.

---

## Task M7-1: GoldenCase V1.1 loader + dataset provenance

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/loader.py`
- Create: `packages/eval/tests/test_v1_loader.py`

**Step 1: Write the failing test**
- `load_golden_cases(path) -> list[GoldenCase]` validates every field per eval TSD §6 (case_id, ticker, session_date, cutoff, question without answer hints, oracle_status, acceptable_cause_labels, evidence_judgments with role/support/materiality/temporal/independence group, expected_primary_evidence, expected_refusal_reason, expected_attribution_type, expected_research_behavior, notes, dataset_version, lineage).
- Rejects: `INSUFFICIENT` as oracle_status (read-only legacy input), missing `expected_refusal_reason` on ABSTAIN cases, `MARKET_STRUCTURE_UNSUPPORTED` used as CauseType/AttributionType, case hash mismatch with dataset manifest.
- `dataset_provenance(manifest, cases) -> str` content hash + lineage; case file immutable by content hash.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** loader (eval-owned; production packages never import it).

**Step 4: Run tests** → PASS.

**Step 5: Commit**
```bash
git add packages/eval/catalyst_eval/v1_1/loader.py packages/eval/tests/test_v1_loader.py
git commit -m "feat(eval): V1.1 GoldenCase loader with strict validation"
```
**Rollback:** `git revert`.

## Task M7-2: Stage-1 12-case dataset (human review) + stratification manifest

**Files:**
- Create: `packages/eval/golden_set/v1_1_stage1_cases.jsonl` (12 rows)
- Create: `packages/eval/golden_set/v1_1_stage1_stratification.json`
- Create: `packages/eval/golden_set/v1_1_stage1_dataset_manifest.json`
- Create: `packages/eval/golden_set/README.md` (update: V1.1 Stage-1 binding)
- Create: `packages/eval/tests/test_stage1_dataset.py`

**Step 1: Write the failing test**
- Exactly 12 cases; category counts match the stratification manifest (sufficient/partial/abstain; positive/negative moves; company-specific/macro/sector; direct-primary; no-material; at least one recoverable corrective case and one multi-gap recoverable case).
- Every case has `parent_case_id` referencing a legacy T4/golden source (g006, g013, g017, g024, g041, g007, h001, h004, h005, h007 or pre_b6 IDs) or a Stage-1 slot id c01–c12, rather than overwriting historical files. Q-011 candidate packets bind slots as c01–c12, so the validator allow-list must match that reality (both the legacy T4/golden names and c01–c12 are accepted).
- No answer-hint leakage in `question` text (assert no expected cause label substring appears verbatim).
- **DATA-02 encoding:** stratification records per-case news serving `content_state` (FULL_TEXT vs TITLE_ONLY/METADATA_ONLY/unrecovered); cases whose news evidence is title-only/metadata-only/unrecovered are marked `coverage_limited`, which makes them non-scorable for **news-attribution efficacy** denominators only. Coverage limitation never excludes a case from the model-failure safety gates (`false_sufficient`, `abstain_producing_sufficient`, `no_material_producing_sufficient`, `no_material_without_sanity`), whose eligibility is defined per metric below.
- Dataset manifest schema is exactly `v1_1_stage1_dataset_manifest_v1` with `dataset_id`, `dataset_version`, ordered `case_ids`, `case_list_sha256`, `dataset_content_sha256`, `case_count=12`, strata counts, per-case source lineage, annotation timestamps, reviewer pseudonymous IDs, second-pass case IDs, `adjudication_state="resolved"`, A3 eligible IDs, A4 readiness-only eligible IDs, approval authority, and `approved_at`. Reject missing/pending approval, duplicate/reordered IDs, or any hash/count mismatch.

**Stratification contract (Q-011 lock, enforced by `validate_stage1_dataset_manifest`):**

- Each `per_case[case_id]` entry adds `challenge_family`, `primary_evidence_kind`, and `move_direction` beside `parent_case_id`, `news_content_state`, and `coverage_limited`.
- `challenge_family` is the scenario/challenge class from Frozen V1.1 §7.2 behaviour classes, with the closed set `COMPANY_SPECIFIC | MACRO | SECTOR | CONTINUATION | DISTRACTOR | QUIET | ABSTENTION`. It is deliberately NOT inferred from `acceptable_cause_labels`; macro/sector/company coverage is proven by challenge-family strata, never by accepted cause labels alone. ABSTAIN cases may be MACRO or SECTOR challenges while carrying no accepted `MACRO_EVENT`/`SECTOR_MOVE` label.
- `primary_evidence_kind` is `FULL_TEXT_BODY | EIGHT_K_SHELL | NONE`. `FULL_TEXT_BODY` means a substantive material body is present in the pinned corpus and (when direct-primary) is bound by `expected_primary_evidence`. `EIGHT_K_SHELL` means only an EDGAR 8-K cover/Item 2.02/exhibit-index/signature shell without the exhibit body; it is never material primary evidence and may be truthfully recorded only while `expected_primary_evidence` stays empty. `NONE` means no local material primary body. `direct_primary` means non-empty `expected_primary_evidence` AND `primary_evidence_kind == FULL_TEXT_BODY`; it is a recorded split, not an exact quota. SUFFICIENT does NOT require local `FULL_TEXT_BODY`.
- ABSTAIN cases MUST have `expected_refusal_reason` and empty `acceptable_cause_labels`, and MUST keep a `challenge_family`.
- `move_direction` records the signed-session direction (positive/negative/mixed) even when accepted labels are empty.
- Validator gates on the 12-case set (fail-closed): exactly 12 unique ordered cases; declared manifest/stratification aggregate strata MUST equal independently recomputed per-case strata (every declared aggregate key, not only coverage); at least one SUFFICIENT, at least one PARTIAL, and at least one ABSTAIN; at least one positive and one negative move; at least one COMPANY_SPECIFIC, one MACRO, and one SECTOR challenge family; at least one `corrective_recoverable=true` case and one recoverable case with ≥2 gap reason codes; every ABSTAIN empty-labelled with a refusal reason; `expected_primary_evidence` non-empty requires `primary_evidence_kind == FULL_TEXT_BODY` and a SUFFICIENT/PARTIAL status; `EIGHT_K_SHELL` with non-empty `expected_primary_evidence` is rejected; ABSTAIN never carries `expected_primary_evidence`; hashes, ordering, lineage, leakage, and approval-required rules stay; the official validator still rejects missing `approval_authority`/`approved_at`.

Public-world vs local coverage:
- `oracle_status` is the human adjudication based on cutoff-valid public evidence. It is NOT inferred from local corpus completeness.
- `coverage_limited`, `news_content_state`, and `primary_evidence_kind` describe the pinned local corpus.
- coverage-limited cases are non-scorable for news-attribution efficacy denominators only; `coverage_limited` never suppresses the ABSTAIN/false-SUFFICIENT safety gates and never removes a case from `false_sufficient` (denominator = all 12 Stage-1 cases).
- `expected_primary_evidence` controls primary-source retrieval/citation eligibility only; it must not suppress ABSTAIN or false-SUFFICIENT safety gates (regression c04: ABSTAIN + coverage_limited + empty `expected_primary_evidence`, model output SUFFICIENT, must fail both `false_sufficient` and `abstain_producing_sufficient`).
- a public-world SUFFICIENT oracle MUST NOT be downgraded solely because the local corpus lacks its material source.
- A Stage-1 fixture MUST include a regression case equivalent to c01: externally SUFFICIENT, locally coverage_limited, no local material primary ID, empty `expected_primary_evidence`.

EIGHT_K_SHELL:
- may truthfully record a shell present in the local corpus
- is never material primary evidence
- must not count as primary retrieval success
- `expected_primary_evidence` remains empty unless bound to an actual substantive FULL_TEXT body
- does not by itself determine or downgrade human oracle_status
- SUFFICIENT does NOT require local FULL_TEXT_BODY
- `EIGHT_K_SHELL` + non-empty `expected_primary_evidence` is still rejected

**Step 2: Run test to verify it fails** → FAIL (dataset absent or gates unmet).

**Step 3: Human annotation workflow (operator)** per eval TSD §7: freeze case IDs + stratification before results; generate identity-bound pool per case under pinned data runtime + four arms; review without model output; second pass on all abstain/conflict/multi-gap/low-confidence cases; lock content hash + publish manifest. One annotator + documented second pass is acceptable for Stage-1.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/golden_set/v1_1_stage1_cases.jsonl packages/eval/golden_set/v1_1_stage1_stratification.json packages/eval/golden_set/v1_1_stage1_dataset_manifest.json packages/eval/golden_set/README.md packages/eval/tests/test_stage1_dataset.py`
`git commit -m "data(eval): V1.1 Stage-1 twelve-case human-reviewed dataset"`

## Task M7-3: EvalManifest builder (append-only identity)

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/manifest_builder.py`
- Modify: `packages/eval/catalyst_eval/v1_1/manifest.py`
- Create: `packages/eval/tests/test_v1_manifest_builder.py`

**Step 1: Write the failing test**
- `build_eval_manifest(*, dataset, stage, split, code_identity, data_runtime_identity, agent_policy, retrieval_policy, metric_spec, eligible_experiments) -> EvalManifest` writes the locked pre-execution identity and an empty planned `run_artifact_identity`; identity fields are immutable after append.
- Reconcile the M2 contract in `packages/eval/catalyst_eval/v1_1/manifest.py`: add required `CaseResultRef.run_manifest_hash` and required `EvalOutcome.observed_run_artifact_identity`; do not add a second manifest schema.
- Data identity references `DataRuntimeIdentity` by ref/hash (never a second copy); eligible experiments predeclare predicate/version + ordered eligible IDs + locked eligibility hash + minimum denominator. `observed_denominator` may be written only in the outcome/report, not retrofitted into pre-execution identity.
- Test canonical formulas, same-input determinism, model/data/order sensitivity, pre-submit capture ordering, one-to-one case binding, and rejection of missing/duplicate/reordered/extra bindings. Outcome append must not change `eval_id`.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** builder.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/manifest_builder.py packages/eval/catalyst_eval/v1_1/manifest.py packages/eval/tests/test_v1_manifest_builder.py`
`git commit -m "feat(eval): append-only EvalManifest builder"`

## Task M7-4: Hidden-gold isolation + leakage scan (production runtime)

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/leakage.py`
- Create: `packages/eval/tests/test_v1_leakage.py`

**Step 1: Write the failing test**
- `scan_run_for_gold(run_artifacts, gold_case) -> list[str]` flags forbidden oracle fields in production prompts, state, traces, and model-visible artifacts: oracle/expected statuses, accepted cause labels, expected research behavior, reviewer notes, relevance/materiality/independence annotations, expected refusal, and hidden answer text.
- Evidence IDs that arrive naturally through the pinned retrieval result and ContextPack inventory are allowed and are later scored. An evidence ID is a leakage violation only when copied from hidden-gold input into runtime without a matching served retrieval record. Findings contain field/path/reason but never repeat secret or hidden-gold values.
- `verify_hidden_gold_boundary()` static check: agents/app/data-core source has no import of `catalyst_eval.golden_set` or `v1_1.case`.
- Eval joins run results to cases only after execution by opaque eval-run binding (never preloads gold into runtime).

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** scans; run leakage scan on every Stage-1 report before promotion.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/leakage.py packages/eval/tests/test_v1_leakage.py`
`git commit -m "feat(eval): hidden-gold isolation and leakage scan"`

## Task M7-5: Retrieval metrics over PoolManifest

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/retrieval_metrics.py`
- Create: `packages/eval/tests/test_v1_retrieval_metrics.py`

**Step 1: Write the failing test**
- Metrics per Final TSD §22.2 / Frozen §10: Recall@K (K=8), MRR, nDCG@K, primary-source hit rate, duplicate-adjusted Precision@8, independent-group recall, source diversity, reranker contribution, cutoff/ticker violations, latency/degradation.
- Human `GoldenCase.evidence_judgments` + identity-bound PoolManifest are ground truth; runtime grouping is never its own oracle (retrieval over pinned human independence relation and algorithm eval are separate metrics).
- Every metric publishes numerator/denominator/eligible/excluded/non-scorable counts + case IDs/manifest; integer case counts with percentages.
- Initial 12-case retrieval gates: Recall@8 ≥ 0.75, primary-source hit ≥ 0.80 where primary evidence exists, duplicate-adjusted Precision@8 ≥ 0.60, ticker/cutoff violations = 0.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** metrics over fixture pools; reuse existing `metrics/*` where compatible.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/retrieval_metrics.py packages/eval/tests/test_v1_retrieval_metrics.py`
`git commit -m "feat(eval): Stage-1 retrieval metrics with denominators"`

## Task M7-6: Attribution + trajectory metrics

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/attribution_metrics.py`
- Create: `packages/eval/catalyst_eval/v1_1/trajectory_metrics.py`
- Create: `packages/eval/catalyst_eval/v1_1/output_audit.py`
- Create: `packages/eval/tests/test_v1_attribution_metrics.py`
- Create: `packages/eval/tests/test_v1_trajectory_metrics.py`

**Step 1: Write the failing test**
- Attribution: citation correctness (100% hard gate), evidence precision, causal relevance, material coverage, unsupported primary causal claims = 0, unsupported material claims ≤ 5%, false SUFFICIENT ≤ 1 of 12, explicit ABSTAIN producing SUFFICIENT = 0, NO_MATERIAL_PUBLIC_CATALYST producing SUFFICIENT = 0, NO_MATERIAL without completed sanity tasks = 0, status confusion matrix, refusal correctness, latency/tokens/cost.
- `v1_1_stage1_output_audit_v1` contains eval ID, case ID, run-manifest ID/hash, result-artifact ID/hash, auditor pseudonymous ID, audited-at UTC, and per-claim claim ID, material flag, citation IDs, resolution/support decision, and reason code. It is accepted only when every material claim is audited, identities match sealed run artifacts, and adjudication is resolved. Generated claims/artifacts are immutable during audit. LLM-judge output is a separate diagnostic artifact.
- **DATA-02 scoring rule:** a news case whose serving text is title-only/metadata-only/unrecovered is **coverage-limited / non-scorable for news-attribution efficacy metrics** (Final TSD §5.2/§5.3/§22, DATA-02 lock). Coverage limitation is never misreported as an Analyst failure, and missing news-body efficacy is never misattributed to the model. Model-failure safety gates use explicit per-metric denominators and are never suppressed by coverage limitation or by empty `expected_primary_evidence`:
  - `false_sufficient`: numerator = cases where `oracle_status != SUFFICIENT` and `output_status == SUFFICIENT`; denominator/eligible_count = all 12 Stage-1 cases (locked “<= 1 of 12” formula).
  - `abstain_producing_sufficient`: denominator/eligible_count = ABSTAIN oracle cases only.
  - `no_material_producing_sufficient`: denominator/eligible_count = cases whose `expected_attribution_type == NO_MATERIAL_PUBLIC_CATALYST`.
  - `no_material_without_sanity`: denominator/eligible_count = runs producing `NO_MATERIAL_PUBLIC_CATALYST`.
`coverage_limited` and `model_limited` reporting stay separate; `expected_primary_evidence` controls primary-source retrieval/citation eligibility only and must not suppress ABSTAIN or false-SUFFICIENT safety gates.
- Trajectory: corrective trigger precision/recall (denominator = human-labelled recoverable/required cases), gap/action match, stop correctness, useful-corrective rate (denominator = predeclared human-labelled corrective_required=true AND corrective_recoverable=true eligible subset); unnecessary-corrective rate (denominator = not-required cases), zero-new-structure rate, paired attribution delta (no-corrective vs one-action on A3 subset).
- A3/A4: EvalManifest binds eligible-subset + `minimum_eligible_denominator` before outcomes; below-minimum runs report diagnostics only, cannot satisfy retention/promotion.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** metrics (pure, fixture-scored).

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/attribution_metrics.py packages/eval/catalyst_eval/v1_1/trajectory_metrics.py packages/eval/catalyst_eval/v1_1/output_audit.py packages/eval/tests/test_v1_attribution_metrics.py packages/eval/tests/test_v1_trajectory_metrics.py`
`git commit -m "feat(eval): attribution and trajectory metrics with hard gates"`

## Task M7-7: A1–A5 experiment seams + Stage-1 development signals

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/ablation.py`
- Create: `packages/agents/catalyst_agents/runtime/experiment.py`
- Modify: `packages/agents/catalyst_agents/graph.py`
- Modify: `packages/app/catalyst_app/runtime/composition.py`
- Create: `packages/eval/tests/test_v1_ablation.py`
- Create: `packages/agents/tests/test_experiment_policy.py`
- Modify: `packages/app/tests/test_runtime_lifespan.py`

**Step 1: Write the failing test**
- A1 (packing: `critic_prefix_600chars_v1` vs `evidence_context_pack_v1`), A2 (hypothesis: `legacy_single_candidate_v1` vs `bounded_competition_v1`), A3 (`max_corrective_rounds` 0 vs 1), A5 (observation: `context_builder_v1_limited` vs `move_profile_v1`) each change exactly one policy seam; all other fixed controls equal; artifacts record policy versions.
- A4: schema + eligibility-manifest readiness only. Validate the predeclared multi-gap subset, predicate version, minimum denominator, and refusal reasons. Do **not** introduce or execute a `max_actions_per_batch=2` arm in M7; executable A4 is Stage-2+ only.
- Stage-1 A1/A2/A3/A5 outputs are development signals only (implementation/development probe); a strong A3 efficacy claim requires Stage-2-or-later.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** immutable `ExperimentPolicyOverride` with explicit A1/A2/A3/A5 fields and an eval capability token owned by the caller. `run_v1_graph` consumes the optional override at the named policy seams; `None` preserves M6 behavior. The eval adapter supplies it, while app production composition rejects any non-`None` override. No case ID or gold label enters the override.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/ablation.py packages/agents/catalyst_agents/runtime/experiment.py packages/agents/catalyst_agents/graph.py packages/app/catalyst_app/runtime/composition.py packages/eval/tests/test_v1_ablation.py packages/agents/tests/test_experiment_policy.py packages/app/tests/test_runtime_lifespan.py`
`git commit -m "feat(eval): A1-A5 experiment seams and Stage-1 development signals"`

## Task M7-8: Stage-1 runner + report (identity-bound)

**Files:**
- Create: `packages/eval/catalyst_eval/v1_1/runner.py`
- Create: `packages/eval/catalyst_eval/v1_1/report.py`
- Create: `packages/eval/catalyst_eval/v1_1/execution_ledger.py`
- Create: `packages/eval/scripts/run_v1_1_stage1.py`
- Create: `packages/eval/tests/test_v1_runner.py`
- Create: `packages/eval/tests/test_v1_report.py`
- Create: `packages/eval/tests/test_v1_stage1_cli.py`

**Step 1: Write the failing test**
- `run_stage1(manifest, cases, runner_adapter, metrics) -> Stage1Report` executes through the M6 app/SSE boundary, captures the pre-submit RunManifest binding, waits for a terminal event, verifies artifact hashes, and appends exactly one outcome only after the complete ordered case set exists.
- The repo-owned CLI is `.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py {prepare,execute,audit,report,all}`. `prepare` performs no provider calls. `execute` requires explicit `--max-provider-calls` and `--max-cost-usd`, refuses an existing incompatible ledger, and resumes only identity-valid terminal cases. `audit` validates a human-provided audit file but cannot author human decisions. `report` writes only from sealed inputs. Exit codes: 0 success, 1 gate failure, 2 contract/operator/configuration error.
- Execution ledger rows bind eval/case/run/result identities, terminal status, attempts, provider calls, cost, and checksums. A nonterminal or corrupt row is never silently reused; completed valid rows are never rerun or billed twice.
- Report JSON uses canonical bytes and write-once publication: absent target creates atomically; identical bytes are idempotent; differing bytes raise conflict without overwrite. Markdown is a deterministic rendering of JSON and cannot supply new facts. `<sha8>` is the first eight characters of the execution code HEAD captured before live execution.
- Tests cover byte reproducibility, resume, incompatible ledger, cost/provider ceilings, missing human audit, identity mismatch, report conflict, secret scan, hard-gate failure report, and no network calls in `prepare`.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** runner + report.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/runner.py packages/eval/catalyst_eval/v1_1/report.py packages/eval/catalyst_eval/v1_1/execution_ledger.py packages/eval/scripts/run_v1_1_stage1.py packages/eval/tests/test_v1_runner.py packages/eval/tests/test_v1_report.py packages/eval/tests/test_v1_stage1_cli.py`
`git commit -m "feat(eval): Stage-1 runner and reproducible report"`

## Task M7-9: T4/user-smoke regression + migration regression

**Files:**
- Modify: `packages/eval/catalyst_eval/post_import/user_smoke.py` (record V1.1 identity)
- Create: `packages/eval/catalyst_eval/v1_1/migration_regression.py`
- Create: `packages/eval/tests/test_v1_migration_regression.py`

**Step 1: Write the failing test**
- T4 and user-smoke remain identity-bound regression/smoke suites (not attribution quality evidence): T4 10-case pack + `FOUR_ARM_E2E_OK` + user-smoke Wave-2 artifact package must still pass on the repaired generation; any regression in refusal semantics/status normalization/technical-failure classification reported separately.
- Migration regression reads the sealed M1 baseline and M5 Gate A/B artifacts through their existing adapters, then runs the M6 V1 app/SSE output on identical request facts. It compares only the locked structural invariants (request facts, runtime identity, ContextPack identity, claim lineage, status/refusal normalization, leakage), declares `comparability_declared=false` whenever data/model/environment identity differs, and makes no direct quality/equality claim. Legacy eval remains sealed baseline reader only.

**Step 2: Run test to verify it fails** → FAIL.

**Step 3: Implement** wrapper + adapter.

**Step 4: Run tests** → PASS.

**Step 5: Commit**
`git add packages/eval/catalyst_eval/v1_1/migration_regression.py packages/eval/catalyst_eval/post_import/user_smoke.py packages/eval/tests/test_v1_migration_regression.py`
`git commit -m "feat(eval): T4/user-smoke regression and migration regression gate"`

## Task M7-10: Operator Stage-1 execution + human audit + report (EXPENSIVE)

**Files:**
- Create: `data/baseline/reports/v1_1_stage1_<sha8>_eval_manifest.json`
- Create: `data/baseline/reports/v1_1_stage1_<sha8>_output_audit.jsonl`
- Create: `data/baseline/reports/v1_1_stage1_<sha8>.json`
- Create: `data/baseline/reports/v1_1_stage1_<sha8>.md`
- Runtime-only: `data/eval_reports/v1_1_stage1_<sha8>/` or `.local/` for ledgers, responses, traces, pools, and provider artifacts; never stage these.

`<sha8>` is the first eight characters of the clean M7 execution HEAD before provider calls. Q-011 approval, exact Q-001 operational derivative identity, explicit provider-call/cost ceilings, and required credentials must exist before `execute`. Q-002 may remain unrecovered only when every manifest/report says NON-COMPARABLE with the documented reason.

**Step 1: Prepare without provider calls**

```bash
.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py prepare \
  --dataset-manifest packages/eval/golden_set/v1_1_stage1_dataset_manifest.json \
  --output-dir data/eval_reports/v1_1_stage1_<sha8> \
  --max-provider-calls <operator-approved-integer> \
  --max-cost-usd <operator-approved-decimal>
```

This verifies the approved dataset/hash/strata, M3 runtime pointers, exact DB/index/model identities, clean execution SHA, provider configuration without printing the credential, empty-or-compatible ledger, experiment eligibility, and disk/runtime readiness. It emits a non-secret prepare summary. Exit 2 means stop; it never writes a success report.

**Step 2: Execute and resume through the live M6 app/SSE path**

```bash
.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py execute \
  --output-dir data/eval_reports/v1_1_stage1_<sha8>
```

Run the default 12 cases and the permitted A1/A2/A3/A5 development arms. Never run A4. Reuse only checksum- and identity-valid terminal ledger rows. Confirm every case has one authoritative RunManifest binding, terminal result, ContextPack/claim/assurance refs, leakage scan result, and measured latency/tokens/cost. A failed case or exceeded ceiling stops further provider dispatch but preserves resumable evidence.

**Step 3: Human output audit**

Give the immutable run/result artifacts—not hidden prompts—to the authorized reviewer. The reviewer completes `v1_1_stage1_<sha8>_output_audit.jsonl`. Then run:

```bash
.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py audit \
  --output-dir data/eval_reports/v1_1_stage1_<sha8> \
  --audit data/baseline/reports/v1_1_stage1_<sha8>_output_audit.jsonl
```

The command validates completeness, identity binding, claim coverage, reviewer/adjudication state, and hashes. It does not generate, infer, or repair human decisions. Missing authority is a legitimate operator stop.

**Step 4: Seal report**

```bash
.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py report \
  --output-dir data/eval_reports/v1_1_stage1_<sha8> \
  --audit data/baseline/reports/v1_1_stage1_<sha8>_output_audit.jsonl \
  --manifest-out data/baseline/reports/v1_1_stage1_<sha8>_eval_manifest.json \
  --json-out data/baseline/reports/v1_1_stage1_<sha8>.json \
  --markdown-out data/baseline/reports/v1_1_stage1_<sha8>.md
```

The report must include all locked integer gates and denominators, retrieval metrics, coverage-limited vs model-limited counts, A1/A2/A3/A5 development signals, A4 readiness only, T4/user-smoke/migration regression, identity/comparability fields, leakage/secret scan, cost/latency, and checksums. A hard-gate failure may publish an explicit failed report but never a passing seal or promotion claim.

**Step 5: Branch exit verification and commit**

```bash
# Run the standing worktree import assertion first.
.venv/bin/python -m pytest packages/eval -q -m "not integration"
.venv/bin/python -m pytest packages/agents -q -m "not integration"
.venv/bin/python -m pytest packages/app -q -m "not integration"
.venv/bin/python packages/eval/scripts/run_v1_1_stage1.py report \
  --output-dir data/eval_reports/v1_1_stage1_<sha8> \
  --audit data/baseline/reports/v1_1_stage1_<sha8>_output_audit.jsonl \
  --manifest-out data/baseline/reports/v1_1_stage1_<sha8>_eval_manifest.json \
  --json-out data/baseline/reports/v1_1_stage1_<sha8>.json \
  --markdown-out data/baseline/reports/v1_1_stage1_<sha8>.md
git diff --check
git status --short
```

The second report command must be byte-identical/idempotent. Stage only the four authoritative artifacts, never `git add -A`:

```bash
git add data/baseline/reports/v1_1_stage1_<sha8>_eval_manifest.json \
  data/baseline/reports/v1_1_stage1_<sha8>_output_audit.jsonl \
  data/baseline/reports/v1_1_stage1_<sha8>.json \
  data/baseline/reports/v1_1_stage1_<sha8>.md
git commit -m "chore(eval): seal V1.1 Stage-1 report <sha8>"
```

**Step 6: Stop for supervisor review**

Report starting/final HEAD, commit list, exact test/gate outputs, dataset/runtime/eval identities, case/arm counts, provider calls/cost, artifact paths/checksums, hard gates, comparability, leakage results, and Git status. Do not merge, tag, push, delete the branch, remove the worktree, or start M8. Supervisor owns integration and annotated tag `v1.1-m7-stage1`.

**Rollback:** re-run the sealed prior generation/path by manifests and label non-comparable identities. Never delete or rewrite gold datasets, human audits, or sealed reports.

## Batch order and review policy

| Batch | Tasks | Continuation rule |
|---|---|---|
| A | M7-0 | Eval FAST must exit 0 |
| B | M7-1, M7-3–M7-9 plus fixture-only M7-2 support | Continue automatically under TDD; stop on contradiction |
| C | M7-2 authoritative human seal, then M7-10 prepare/execute/audit/report | Requires the documented operator/human inputs |

There is no routine mid-batch supervisor review. Execute all automatically available work efficiently, then stop only at a documented external-authority gate, hard failure, or the final M7 review point.

## Exit gate summary (M7)

- [ ] Approved 12-case human-reviewed dataset + dataset/stratification manifests; no leakage
- [ ] EvalManifest append-only with predeclared denominators and observed run bindings
- [ ] Retrieval/attribution/trajectory metrics with mandatory denominators
- [ ] A1/A2/A3/A5 dev signals; A4 readiness only
- [ ] T4/user-smoke + migration regression green
- [ ] Hard gates reported with integer counts; non-comparability markers
- [ ] Human output audit complete; reproducible Stage-1 manifest/report sealed
- [ ] Supervisor review passes; annotated tag `v1.1-m7-stage1` then created by supervisor
