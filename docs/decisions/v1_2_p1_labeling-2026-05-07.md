# v1_2_p1_set Labeling Note (2026-05-07)

## Scope
- Output file: `packages/eval/golden_set/v1_2_p1_set.jsonl`
- Source pool: `packages/eval/golden_set/v1_2.jsonl` (50 rows, `g001`..`g050`)

## Label source and rules
1. Base rows are copied from `v1_2.jsonl` in the same order (no row drops/additions).
2. For IDs overlapping frozen P0 labeled subset (`v1_2_p0_set.jsonl`), labels are inherited as-is:
   - `expected_status`
   - `should_refuse`
3. For non-overlap IDs, labels are set by conservative default:
   - `expected_status = "SUFFICIENT"`
   - `should_refuse = false`

## Rationale
- Preserve already-reviewed P0 labels where available.
- Produce a fully labeled 50-case file required by decision-grade T11 runner strict mode.
- Avoid silent schema fallback in evaluation code.

## Resulting distribution
- Total rows: 50
- `expected_status`:
  - `SUFFICIENT`: 46
  - `PARTIAL`: 2
  - `INSUFFICIENT`: 2
- `should_refuse=true`: 2
